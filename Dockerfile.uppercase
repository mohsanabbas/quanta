# Uppercase transformer container using host-built static linux binary
# Build the binary on host: make build-linux ARCH=arm64|amd64
FROM scratch

ARG BIN_DIR=bin/linux-amd64
COPY ${BIN_DIR}/uppercase /uppercase

EXPOSE 50052
ENTRYPOINT ["/uppercase","--listen=:50052"]
